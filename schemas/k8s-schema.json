{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "PingPongKong K8s Matrix Schema",
  "type": "object",
  "required": ["version", "cluster", "environment", "description", "topology", "matrix"],
  "additionalProperties": false,
  "properties": {
    "version": { 
      "type": "string" 
    },
    "cluster": { 
      "type": "string" 
    },
    "environment": {
      "type": "string"
    },
    "description": {
      "type": "string"
    },
    "topology": {
      "type": "object",
      "required": ["node-labels"],
      "additionalProperties": false,
      "properties": {
        "node-labels": { 
          "type": "object", 
          "additionalProperties": { "type": "string" },
          "minProperties": 1
        }
      }
    },
    "matrix": {
      "type": "object",
      "required": ["internal", "external"],
      "additionalProperties": false,
      "properties": {
        "internal": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["from", "to", "ports", "protocol", "action", "description"],
            "additionalProperties": false,
            "properties": {
              "description": { "type": "string" },
              "from": { "type": "string" },
              "to": { "type": "string" },
              "ports": {
                "type": "array",
                "items": { "type": "integer", "minimum": 1, "maximum": 65535 },
                "minItems": 1
              },
              "protocol": { "type": "string", "enum": ["tcp", "udp", "icmp"] },
              "action": { "type": "string", "enum": ["allow", "deny"] }
            }
          }
        },
        "external": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["name", "from", "endpoint", "protocol", "action", "description"],
            "additionalProperties": false,
            "properties": {
              "description": { "type": "string" },
              "name": { "type": "string" },
              "from": { "type": "string" },
              "endpoint": { "type": "string" },
              "protocol": { "type": "string", "enum": ["tcp", "udp", "icmp"] },
              "action": { "type": "string", "enum": ["allow", "deny"] }
            }
          }
        }
      }
    }
  }
}
